package main

// import ()

// type SDPCoordinator interface {
// 	Connect(serverAddr string, serverPort int) error
// 	CheckForNewPeerOffers(peerPrefix string) string[] // returns any new sdp offers (base64 encoded)
// 	SetAnswer(peerKey string, sdpAnswer) error // sets the sdp answer for a given peer
// 	Disconnect() error
// }
